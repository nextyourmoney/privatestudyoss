package Algorithm_0310;

//https://www.acmicpc.net/problem/9093
import java.util.ArrayList;
import java.util.Scanner;

public class bronze1_9093 {

	/*
	 * public static void main(String[] args) { Scanner scanner = new
	 * Scanner(System.in); int num = Integer.parseInt(scanner.nextLine()); String[]
	 * results = new String[num];
	 * 
	 * for (int i = 0; i < num; i++) { String contents = scanner.nextLine();
	 * String[] wordArr = contents.split(" ");
	 * 
	 * String resultStr = " "; ArrayList result = new ArrayList();
	 * 
	 * for (String word : wordArr) { for (int j = word.length(); j > 0; j--) {
	 * 
	 * resultStr += word.charAt(j-1);
	 * 
	 * 
	 * } resultStr += " "; } results[i] = resultStr.toString(); }
	 * 
	 * for (String result : results) { System.out.println(result); } }
	 */
}
